#include "Map.h"

Map::Map()
{
    loadPlatforms();
}

Map::loadPlatforms()
{
    // From file ...
    for () {
        Platform platform(1, 2, 3, 4, ...);
        m_platforms.push_back(platform);
    }
}

